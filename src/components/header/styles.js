import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
  container: {
    height: 80,
    backgroundColor: 'red',
    marginTop: 30,
    justifyContent: 'center',
    alignItems: 'center'
  },
  title: {
    fontSize: 20,
    color: "#fff",
    fontWeight:'bold'
  },
});
